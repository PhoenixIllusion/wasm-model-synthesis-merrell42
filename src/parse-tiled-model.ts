import { ParsedInputBase, ParsedInputHead } from "./parse-input";

export interface TiledModelConfig {
  name: string;
}
export async function parseTiledModel(node: Element, head: ParsedInputHead, config: TiledModelConfig): ParsedInputBase {

}